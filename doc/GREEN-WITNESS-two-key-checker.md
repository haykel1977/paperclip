# TEMPORARY GREEN WITNESS — two-key paperclip checker

> **This file is a temporary, harmless documentation-only marker.** It exists
> solely to open a pull request that exercises the two-key paperclip checker
> (`paperclip-checker/app` + `paperclip-checker-runner`) against a real PR head,
> proving both exact-head checks publish and turn green while
> `PAPERCLIP_CHECKER_ENABLED=true`.
>
> It touches no workflows, config, code, or secrets. It may be deleted once the
> witness has served its purpose. The PR may be merged **only if** both checker
> keys and all other required checks succeed on the exact head SHA.

Created for operational verification on 2026-07-23.
